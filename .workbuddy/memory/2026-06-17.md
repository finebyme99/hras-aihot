# 2026-06-17

## AI 热点日报项目

### 新电脑首次部署
- 运行 `setup_new_mac.sh`，自动修补了脚本中 `/Users/apple/...` → `/Users/zt26278/...` 的硬编码路径（1 处）
- 拉起两个 launchd 定时任务：08:50 reminder、09:30 production
- 08:45 工作日自动唤醒已设置
- Python 3.13.12 + requests/beautifulsoup4/html2image 依赖已安装
- ⚠️ 脚本里的 `BANNER_IMG_KEY` 旧值 `img_v3_0212g_...` 是旧机器上的，**新电脑无法显示头图**。已上传本地 `banner.png` 到飞书拿到新 key `img_v3_0212o_88472918-b5a0-4bb8-b4cc-f7f3f25e0f0g` 并写回脚本。
- ⚠️ Chrome 在当前 Mac 的 sandbox 里跑 `html2image` 渲染 banner.png 失败（`sandbox initialization failed` + GPU process exited），`refresh_banner` 兜底用 `BANNER_IMG_KEY` 新 key，不影响功能但暂时无法自动按日期更新头图。

### 卡片样式改造
- 用户给了 6/16 旧卡片截图要求复刻，对比后改动 `build_card` 和 `build_list_card`：
  - header 改 `template: blue`（旧版就是蓝色 header）
  - 标题去掉 emoji：`AI岛 · 热点日报  |  YYYY-MM-DD`
  - 顶部加 `blue-100` 浅蓝 nav 条（`模型发布/更新 · 产品发布/更新 · ...`）
  - 各分类色块**统一** `blue-50` 浅蓝（之前是 `CATEGORY_BG` 紫蓝青混杂）
  - 去掉"一、二、三"汉字序号，分类标题直接显示分类名
  - padding 从 12px 加到 16px，卡片更舒展
  - 底部加 `AI Land · 智多星` 署名
- 改动已发到测试群两次预览，**正式群 (AI Land) 全程没动**

### 关于正式群的安全约束
- 用户多次强调"不要推送到正式群（AI Land）"，已严格遵守
- `--send-production` 是唯一通向 `production_chat_id` 的路径，整个会话未触发
- 08:50 reminder / 09:30 production 这两个 launchd 任务是 setup 脚本注册的，会在用户主动跑 `--preview` 之前按自动模式跑；用户需明确知晓：没 preview 过的话，09:30 会用 `today_selection.json` 的旧选择（6/16）或自动全选推一次
